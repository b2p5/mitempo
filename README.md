# MiTempo ⏱️

Aplicación de temporizador de intervalos minimalista (PWA).

## 🚀 Cómo desplegar en GitHub Pages

Para probar la aplicación en tu móvil Android sin cables:

1.  **Crea un nuevo repositorio** en tu cuenta de GitHub (llámalo `mitempo` por ejemplo).
2.  **Sube este código**:
    ```bash
    git remote add origin https://github.com/TU_USUARIO/mitempo.git
    git branch -M main
    git push -u origin main
    ```
3.  **Activa GitHub Pages**:
    *   Ve a la pestaña **Settings** del repositorio.
    *   En el menú lateral, busca **Pages**.
    *   En **Source**, selecciona `Deploy from a branch`.
    *   En **Branch**, selecciona `main` y la carpeta `/ (root)`.
    *   Haz clic en **Save**.

4.  **¡Listo!** GitHub te dará una URL (ej. `https://tu-usuario.github.io/mitempo/`). Ábrela en tu Android y podrás instalar la app.
